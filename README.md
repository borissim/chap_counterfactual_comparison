# chap_counterfactual_comparison
Tutorial on how to run the `chap causal` basic CLI command. Outputs a comparison plot on model prediction over two datasets, observed (original) and modified (counterfactual)
